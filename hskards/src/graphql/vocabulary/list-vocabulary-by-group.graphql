query ListVocabularyByGroup($id: Int!) {
    vocabularyGroup(lessonId: $id) {
        lessonId
        character {
            simplifiedChinese
            pinyin
            english
        }
    }
}

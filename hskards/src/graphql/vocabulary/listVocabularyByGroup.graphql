query ListVocabularyByGroup($id: Number!) {
    vocabularyGroup(id: $id) {
        id
        lessonId
        character {
            simplifiedChinese
            pinyin
            english
        }
    }
}
